# Collision Avoidance

## Current implementation

Simple dynamics 2-player case derived from guest lecture head on collision example. Two objects on head on collision path with constant velocity and are only able to control yaw.

```
] activate .
```

```
include("scripts/solve_game.jl)
```